package test

// DeviceList is a root type (unreferenced + exported)
type DeviceList []Device

// Device is a helper type (referenced by DeviceList)
type Device struct {
	Name     string `json:"name"`
	Channels int32  `json:"channels"`
	Rate     int32  `json:"rate"`
}
